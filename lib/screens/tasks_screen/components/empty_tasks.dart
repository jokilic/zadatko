import 'package:flutter/material.dart';

// Gets shown when there are no tasks
class EmptyTasks extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
